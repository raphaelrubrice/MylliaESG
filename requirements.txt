# Core (always needed)
gseapy
pandas>=1.5
numpy>=1.24
scipy>=1.10
python-igraph>=0.11
leidenalg>=0.10

# Co-expression view (optional, needed if using --adata)
scanpy>=1.9
anndata>=0.10

# ESM-2 view (optional, needed unless --skip-esm)
fair-esm>=2.0
torch>=2.0
mygene>=3.2
